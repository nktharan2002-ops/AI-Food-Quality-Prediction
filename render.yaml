services:
  - type: web
    runtime: python
    name: nutrition-analyzer
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --workers 1 --timeout 120 --bind 0.0.0.0:$PORT app1:app
